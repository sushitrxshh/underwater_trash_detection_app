# Essential build tools (always place at the top)
pip>=23.0.1
setuptools>=65.5.0
wheel>=0.38.0

# Streamlit and its core dependencies
streamlit==1.42.2
streamlit-webrtc==0.63.3

# AI/Computer Vision Dependencies
# Pinning to versions known to have stable wheels for Linux/Python 3.9/3.10
# and compatible numpy versions.
opencv-python-headless==4.8.0.76 # A widely compatible version
numpy==1.24.4 # Compatible with opencv 4.8.0.76
Pillow==9.5.0 # A widely compatible version
ultralytics==8.0.196 # A widely compatible version (often pulls its own torch)

# For streamlit-webrtc's video processing
av==11.0.0 # A widely compatible version

# Other common dependencies that might be pulled by Streamlit/WebRTC/Ultralytics
# Keep this minimal.